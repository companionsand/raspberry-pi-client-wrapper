# =============================================================================
# Raspberry Pi Client Wrapper - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# Usage: cp .env.example .env
#
# This file configures both the wrapper installation and passes values to the
# raspberry-pi-client .env file.
#
# =============================================================================

# =============================================================================
# WRAPPER CONFIGURATION (Installation settings)
# =============================================================================

# Device Identification
DEVICE_ID=raspberry-pi-001                           # Unique identifier for this device

# Environment
ENV=production                                       # Environment: production, staging, development

# OpenTelemetry Central Collector
OTEL_CENTRAL_COLLECTOR_ENDPOINT=https://your-collector.onrender.com:4318
                                                     # Central OTEL collector endpoint

# Git Configuration
GIT_BRANCH=main                                      # Branch to use for raspberry-pi-client repo

# Audio Setup Mode
SKIP_ECHO_CANCEL_SETUP=false                        # Skip PipeWire/echo cancellation setup
                                                     # Set to "true" if using ReSpeaker with hardware AEC
                                                     # Set to "false" if using separate USB mic/speaker
                                                     # Default: false (use PipeWire echo cancellation)

# =============================================================================
# CLIENT CONFIGURATION (Passed to raspberry-pi-client/.env)
# =============================================================================

# Supabase Authentication
SUPABASE_URL=https://your-project.supabase.co       # Your Supabase project URL
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6... # Supabase anonymous/public key
EMAIL=user@example.com                               # User email for authentication
PASSWORD=your_secure_password                        # User password

# Backend
CONVERSATION_ORCHESTRATOR_URL=wss://your-backend.onrender.com/ws
                                                     # WebSocket URL for orchestrator

# API Keys
ELEVENLABS_API_KEY=sk_xxxxxxxxxxxxx                 # ElevenLabs API key for TTS
PICOVOICE_ACCESS_KEY=xxxxxxxxxxxxx                  # Picovoice access key for wake word

# Wake Word
WAKE_WORD=porcupine                                  # Wake word to detect
                                                     # Options: porcupine, jarvis, alexa, etc.

# LED Visual Feedback (for ReSpeaker)
LED_ENABLED=true                                     # Enable/disable LED feedback
LED_BRIGHTNESS=60                                    # LED brightness (0-100)

# =============================================================================
# NOTES
# =============================================================================
#
# Audio Configuration:
# - SAMPLE_RATE is hardcoded to 16000 Hz in raspberry-pi-client (optimized)
# - If SKIP_ECHO_CANCEL_SETUP=false: Uses PipeWire with software echo cancellation
# - If SKIP_ECHO_CANCEL_SETUP=true: Uses ALSA-only with auto-detected devices
#
# Installation Modes:
# 1. Automated (with .env file):
#    - All values from .env are used automatically
#    - No prompts during installation
#
# 2. Interactive (without .env file):
#    - Prompts for DEVICE_ID, ENV, OTEL_CENTRAL_COLLECTOR_ENDPOINT
#    - Prompts for microphone/speaker if using echo cancellation
#    - Creates client .env with placeholders for API keys
#
# Git Repository:
# - Now public: https://github.com/companionsand/raspberry-pi-client.git
# - No SSH keys required!
#
# =============================================================================
